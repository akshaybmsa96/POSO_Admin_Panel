<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akki.daybox_admin_portal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".global.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FullDetailActivity" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="7dd0a29c189d54057b52bf8855c6aed5817f398e" />

        <activity android:name=".activity.AllOrdersActivity" />
        <activity android:name=".activity.ConfirmedOrdersFullDetailsActivity"></activity>
    </application>

</manifest>